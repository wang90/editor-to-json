import { defineComponent } from 'vue'
 
const component = {
  name: 'AiFont',
  props:{
    data: String,
  },
  setup(props,context) {
    // setup 可接受两个参数，一个props，和 context
  }
}
 
export default component